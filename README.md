# kojalang.org

The landing page for [Koja](https://github.com/koja-lang/koja).

A dependency-free static site: plain HTML and CSS, no build step. Open `index.html` in a browser, or serve the directory:

```sh
python3 -m http.server --directory .
```
